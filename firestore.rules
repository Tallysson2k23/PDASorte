rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // O navegador não acessa coleções diretamente no Marco 2.
    // Toda leitura e escrita administrativa passa pelo servidor verificado.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
